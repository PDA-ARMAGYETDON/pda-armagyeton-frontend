import * as S from "./SmallBackground.style";

// eslint-disable-next-line react/prop-types
const SmallBackGroundPage = ({ children }) => {
  return <S.MainDiv>{children}</S.MainDiv>;
};

export default SmallBackGroundPage;
